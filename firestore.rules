service cloud.firestore {
  match /databases/{database}/documents {
  	match /savings/{id} {

      allow list: if false;
      allow get, write;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
